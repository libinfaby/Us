{
  "name": "pingu-codu-api",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "db:migrate:local": "wrangler d1 migrations apply pingu-codu-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply pingu-codu-db --remote"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260911.1",
    "typescript": "^7.0.2",
    "wrangler": "^4.131.0"
  },
  "dependencies": {
    "hono": "^4.13.7"
  }
}
